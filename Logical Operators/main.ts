// NO .4
    //   Logical Operators

       //  && = AND , || = OR , ! = NOT

       // Example of use of logical operators


       let userName :string= "ASAD SHABIR"
       let password : number= 2533

// && means that the userName and password both should be currect || means if any one of them is currect then file will run 

       if (userName == "ASAD SHABIR" && password == 2533) {
        console.log("Login Succesfully")
        
       } else {
        console.log("Invalid username or password !!");
        
        
       }